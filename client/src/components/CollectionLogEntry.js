import React from "react";

const CollectionLogEntry = (props) => {
    const { entry } = props;
    return (
        <div>
            {entry}
        </div>
    )
}

export default CollectionLogEntry;